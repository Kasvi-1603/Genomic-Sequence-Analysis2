services:
  - type: web
    name: igda-viznew
    runtime: python
    plan: free
    buildCommand: pip install -e ".[deploy]"
    startCommand: gunicorn -w 1 --timeout 180 -b 0.0.0.0:$PORT igda.vizNew.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
