services:
  - type: web
    name: igda-viznew
    runtime: python
    plan: free
    buildCommand: pip install -e ".[deploy]"
    startCommand: gunicorn -w 2 --timeout 120 -b 0.0.0.0:$PORT igda.vizNew.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
